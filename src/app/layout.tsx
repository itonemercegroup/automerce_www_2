import type { Metadata } from "next";
import "./globals.css";
import { Header } from "@/components/Header";
import { Footer } from "@/components/Footer";

export const metadata: Metadata = {
  title: "Automerce - Automatyzacja i rozwój e-commerce",
  description: "Nowoczesna firma zajmująca się automatyzacjami, tworzeniem i wdrażaniem oprogramowania oraz wsparciem IT dla sektora e-commerce.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pl">
      <body className="antialiased">
        <Header />
        <main className="min-h-screen">{children}</main>
        <Footer />
      </body>
    </html>
  );
}
